import React, { ChangeEvent, useState } from 'react'


const IngredientField = ({ field, form, type = 'text', label, placeholder }: any) => {

    const { name, value, onChange, onBlur } = field;

    

    return (
        <div>
            
        </div>
    )
}

export default IngredientField
